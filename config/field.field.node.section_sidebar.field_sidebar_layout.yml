uuid: 67a65000-4b64-4c76-9121-59b395450e83
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_sidebar_layout
    - node.type.section_sidebar
  module:
    - options
id: node.section_sidebar.field_sidebar_layout
field_name: field_sidebar_layout
entity_type: node
bundle: section_sidebar
label: 'Sidebar Layout'
description: 'Layout styles are useful when trying to add custom text to an individual sidebar. Default styles are usually used for dynamic content that gets generated by Drupal, e.g., news, facts, reasons. Selecting White Background will add a white background and padding to the text that you enter in the text field. White Background should only be used when adding text to this piece of content and not when selecting from the references list.'
required: false
translatable: false
default_value:
  -
    value: '1'
default_value_callback: ''
settings: {  }
field_type: list_string
