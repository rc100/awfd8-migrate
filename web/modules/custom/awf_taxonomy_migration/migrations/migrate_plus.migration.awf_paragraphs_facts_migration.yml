# langcode: en
status: true
dependencies:
  module:
    - awf_taxonomy_migration
  enforced:
    modules:
      - awf_taxonomy_migration
id: awf_paragraphs_facts_migration
class: null
field_plugin_method: {  }
cck_plugin_method: {  }
migration_tags:
  - 'Drupal 7'
migration_group: awf_migration_group
label: 'AWF Paragraphs Migration'
source:
  plugin: d7_field_collection_item_asg
  field_name: field_facts
process:
  type: 
    plugin: default_value
    default_value: field_facts
  revision_id: revision_id
  field_facts_label: field_fact_label
  field_facts_description:
    plugin: sub_process
    source: field_fact_text
    process:
      value: value
      format: format
    revision_id: revision_id
destination:
  plugin: 'entity_reference_revisions:paragraph'
  # asg change. should be default default_value = facts
  default_bundle: field_facts_paragraph
  # plugin: entity:paragraph
migration_dependencies:
  required: { }
  optional: { }